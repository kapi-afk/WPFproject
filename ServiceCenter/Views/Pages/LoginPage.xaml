<Page x:Class="ServiceCenter.Views.Pages.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:vm="clr-namespace:ServiceCenter.ViewModels"
      mc:Ignorable="d"
      Title="LoginPage"
      Background="{DynamicResource ContentBackgroundBrush}">

    <Page.DataContext>
        <vm:LoginViewModel />
    </Page.DataContext>

    <Grid>
        <Border Style="{StaticResource AuthCardStyle}"
                KeyDown="LoginKeyDown">
            <StackPanel>
                <TextBlock Text="{DynamicResource LoginTitle}"
                           Style="{StaticResource PageTitleStyle}"
                           HorizontalAlignment="Center"/>

                <TextBlock Text="{DynamicResource LoginLabelShort}"
                           Style="{StaticResource AuthLabelStyle}"/>
                <TextBox Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource AuthInputStyle}"/>

                <TextBlock Text="{DynamicResource PasswordPlaceholder}"
                           Style="{StaticResource AuthLabelStyle}"/>
                <PasswordBox Grid.Row="2"
                             x:Name="PasswordBox"
                             PasswordChar="*"
                             Style="{StaticResource AuthPasswordStyle}"
                             Margin="0,0,0,18"/>

                <Button Content="{DynamicResource LoginButton}"
                        Command="{Binding LoginCommand}"
                        Style="{StaticResource AuthPrimaryButtonStyle}"
                        Margin="0,0,0,18"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock>
                        <Hyperlink NavigateUri="RegisterPage.xaml" Command="{Binding NavigateCommand}" CommandParameter="Register">
                            <Run Text="{DynamicResource RegisterLink}"/>
                        </Hyperlink>
                    </TextBlock>
                    <TextBlock Text=" | " Margin="6,0"/>
                    <TextBlock>
                        <Hyperlink NavigateUri="RecoverPage.xaml" Command="{Binding NavigateCommand}" CommandParameter="Recover">
                            <Run Text="{DynamicResource RecoverLink}"/>
                        </Hyperlink>
                    </TextBlock>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Page>
