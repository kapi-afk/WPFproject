<Page x:Class="Printinvest_WPF_app.Views.Pages.CartPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:vm="clr-namespace:Printinvest_WPF_app.ViewModels"
      xmlns:converters="clr-namespace:Printinvest_WPF_app.Converters"
      mc:Ignorable="d"
      Title="CartPage"
      Background="{DynamicResource ContentBackgroundBrush}">

    <Page.DataContext>
        <vm:RepairOrderViewModel />
    </Page.DataContext>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="20" MaxWidth="760">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       Text="Новая заявка на ремонт"
                       FontSize="26"
                       FontWeight="Bold"
                       Foreground="{DynamicResource TextBrush}"
                       Margin="0,0,0,8"/>

            <TextBlock Grid.Row="1"
                       Text="Опишите устройство и неисправность. После создания заявки администратор назначит мастера."
                       TextWrapping="Wrap"
                       Foreground="{DynamicResource TextBrush}"
                       Opacity="0.85"
                       Margin="0,0,0,20"/>

            <GroupBox Grid.Row="2" Header="Данные заявки">
                <StackPanel Margin="16">
                    <TextBlock Text="Тип устройства" Margin="0,0,0,6"/>
                    <ComboBox ItemsSource="{Binding DeviceTypes}"
                              SelectedItem="{Binding DeviceType}"
                              Margin="0,0,0,12"/>

                    <TextBlock Text="Бренд" Margin="0,0,0,6"/>
                    <TextBox Text="{Binding DeviceBrand, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0,0,0,12"/>

                    <TextBlock Text="Модель" Margin="0,0,0,6"/>
                    <TextBox Text="{Binding DeviceModel, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0,0,0,12"/>

                    <TextBlock Text="Телефон для связи" Margin="0,0,0,6"/>
                    <TextBox Text="{Binding ContactPhone, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0,0,0,12"/>

                    <TextBlock Text="Описание неисправности" Margin="0,0,0,6"/>
                    <TextBox Text="{Binding ProblemDescription, UpdateSourceTrigger=PropertyChanged}"
                             AcceptsReturn="True"
                             TextWrapping="Wrap"
                             Height="100"
                             Margin="0,0,0,12"/>

                    <TextBlock Text="Дополнительный комментарий" Margin="0,0,0,6"/>
                    <TextBox Text="{Binding ClientComment, UpdateSourceTrigger=PropertyChanged}"
                             AcceptsReturn="True"
                             TextWrapping="Wrap"
                             Height="80"
                             Margin="0,0,0,16"/>

                    <Button Content="Создать заявку"
                            Command="{Binding CreateOrderCommand}"
                            Width="170"
                            HorizontalAlignment="Left"/>
                </StackPanel>
            </GroupBox>
        </Grid>
    </ScrollViewer>
</Page>
