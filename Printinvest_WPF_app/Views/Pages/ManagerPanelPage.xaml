<Page x:Class="Printinvest_WPF_app.Views.Pages.ManagerPanelPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:vm="clr-namespace:Printinvest_WPF_app.ViewModels"
      xmlns:conv="clr-namespace:Printinvest_WPF_app.Converters"
      mc:Ignorable="d"
      Title="ManagerPanelPage"
      Background="{DynamicResource ContentBackgroundBrush}">

    <Page.DataContext>
        <vm:ServiceMasterPanelViewModel />
    </Page.DataContext>

    <Page.Resources>
        <conv:OrderStatusConverter x:Key="OrderStatusConverter" />
    </Page.Resources>

    <Grid Margin="14,16,14,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="{DynamicResource ManagerPanelTitle}"
                       Style="{StaticResource PageTitleStyle}"
                       VerticalAlignment="Center"/>
        </Grid>

        <TabControl Grid.Row="1">
            <TabItem Header="Ремонтные заявки">
                <Grid Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="340" />
                    </Grid.ColumnDefinitions>

                    <DataGrid ItemsSource="{Binding Orders}"
                              SelectedItem="{Binding SelectedOrder}"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              IsReadOnly="True"
                              Margin="0,0,14,0"
                              HeadersVisibility="Column"
                              GridLinesVisibility="Horizontal"
                              HorizontalScrollBarVisibility="Disabled">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Номер" Binding="{Binding DisplayNumber}" Width="95"/>
                            <DataGridTextColumn Header="Клиент" Binding="{Binding User.Name}" Width="1.1*"/>
                            <DataGridTextColumn Header="Устройство" Binding="{Binding DeviceType}" Width="1.1*"/>
                            <DataGridTextColumn Header="Бренд" Binding="{Binding DeviceBrand}" Width="1*"/>
                            <DataGridTextColumn Header="Модель" Binding="{Binding DeviceModel}" Width="1*"/>
                            <DataGridTextColumn Header="Проблема" Binding="{Binding ProblemDescription}" Width="2*"/>
                            <DataGridTemplateColumn Header="Статус" Width="1.25*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <ContentControl Content="{Binding Status}"
                                                        ContentTemplate="{StaticResource OrderStatusBadgeTemplate}"
                                                        HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>

                    <ScrollViewer Grid.Column="1"
                                  VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Disabled">
                        <Border Style="{StaticResource CardStyle}"
                                Padding="16">
                            <StackPanel>
                                <TextBlock Text="Работа с заявкой"
                                           Style="{StaticResource SectionTitleStyle}"
                                           FontSize="18"
                                           Margin="0,0,0,12"/>
                                <Grid Margin="0,0,0,12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Заявка:" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,8,8"/>
                                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding SelectedOrder.DisplayNumber, TargetNullValue=Не выбрана}" Margin="0,0,0,8"/>

                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Клиент:" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,8,8"/>
                                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding SelectedOrder.User.Name, TargetNullValue=Не выбран}" Margin="0,0,0,8"/>

                                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Устройство:" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,8,0"/>
                                    <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding SelectedOrder.DeviceType, TargetNullValue=Не выбрано}"/>
                                </Grid>

                                <TextBlock Text="Стоимость деталей" Style="{StaticResource FieldLabelStyle}" FontSize="14"/>
                                <TextBlock Text="{Binding SelectedOrder.EstimatedPartsCost, StringFormat={}{0:N2} BYN}"
                                           Margin="0,0,0,8"/>

                                <TextBlock Text="Стоимость работы мастера" Style="{StaticResource FieldLabelStyle}" FontSize="14"/>
                                <TextBox Text="{Binding SelectedOrderMasterWorkCost, UpdateSourceTrigger=LostFocus}"
                                         Margin="0,0,0,8"
                                         Height="30"/>

                                <TextBlock Text="Предварительная сумма" Style="{StaticResource FieldLabelStyle}" FontSize="14"/>
                                <TextBlock Text="{Binding SelectedOrder.EstimatedRepairCost, StringFormat={}{0:N2} BYN}"
                                           Margin="0,0,0,12"/>

                                <TextBlock Text="Новый статус" Style="{StaticResource FieldLabelStyle}"/>
                                <ComboBox ItemsSource="{Binding OrderStatuses}"
                                          SelectedItem="{Binding SelectedOrderStatus}"
                                          ItemTemplate="{StaticResource OrderStatusTemplate}"
                                          Margin="0,0,0,12" Height="30"/>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="10" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>

                                    <Button Grid.Column="0"
                                            Content="Сохранить"
                                            Style="{StaticResource MediumPrimaryButtonStyle}"
                                            Command="{Binding UpdateOrderStatusCommand}"/>
                                    <Button Grid.Column="2"
                                            Content="Открыть склад"
                                            Style="{StaticResource MediumSecondaryButtonStyle}"
                                            Command="{Binding OpenWarehouseWindowCommand}"/>
                                </Grid>
                            </StackPanel>
                        </Border>
                    </ScrollViewer>
                </Grid>
            </TabItem>

            <TabItem Header="Заявки на материалы">
                <Grid Margin="0,10,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <TextBlock Text="Заявки на материалы"
                               Style="{StaticResource SectionTitleStyle}"/>
                    <Grid Grid.Row="1" Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="320" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0"
                                   Text="Поиск по заказу"
                                   Style="{StaticResource FieldLabelStyle}"
                                   VerticalAlignment="Center"
                                   Margin="0,0,8,0"/>
                        <TextBox Grid.Column="1"
                                 Text="{Binding MaterialRequestSearchText, UpdateSourceTrigger=PropertyChanged}"/>
                    </Grid>
                    <DataGrid Grid.Row="2"
                              ItemsSource="{Binding CurrentOrderWarehouseRequests}"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              IsReadOnly="True"
                              HeadersVisibility="Column"
                              GridLinesVisibility="Horizontal">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Заказ №" Binding="{Binding OrderDisplayNumber}" Width="0.9*"/>
                            <DataGridTextColumn Header="Материал" Binding="{Binding RequestedItemName}" Width="2*"/>
                            <DataGridTextColumn Header="Категория" Binding="{Binding RequestedCategory}" Width="1.3*"/>
                            <DataGridTextColumn Header="Кол-во" Binding="{Binding RequestedQuantity}" Width="0.7*"/>
                            <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="1*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Page>
