<Window x:Class="Printinvest_WPF_app.Views.WarehouseRequestsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Заявки мастеров"
        Height="520"
        Width="860"
        MinHeight="520"
        MinWidth="860"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource ContentBackgroundBrush}">

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Заявки мастеров"
                   FontSize="24"
                   FontWeight="Bold"
                   Margin="0,0,0,10"/>

        <TextBlock Grid.Row="1"
                   Text="Здесь отображаются материалы, которые мастера попросили заказать для своих заявок."
                   TextWrapping="Wrap"
                   Foreground="{DynamicResource SecondaryTextBrush}"
                   Margin="0,0,0,12"/>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding WarehouseRequests}"
                  SelectedItem="{Binding SelectedWarehouseRequest}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  HorizontalScrollBarVisibility="Disabled">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Заявка" Binding="{Binding OrderId, StringFormat=№ {0}}" Width="0.8*"/>
                <DataGridTextColumn Header="Мастер" Binding="{Binding Master.Name}" Width="1.2*"/>
                <DataGridTextColumn Header="Материал" Binding="{Binding RequestedItemName}" Width="1.7*"/>
                <DataGridTextColumn Header="Категория" Binding="{Binding RequestedCategory}" Width="1.3*"/>
                <DataGridTextColumn Header="Кол-во" Binding="{Binding RequestedQuantity}" Width="0.8*"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="1.1*"/>
                <DataGridTemplateColumn Header="" Width="110">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Закрыть"
                                    Width="90"
                                    Height="28"
                                    Command="{Binding DataContext.ResolveWarehouseRequestCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                    CommandParameter="{Binding}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
