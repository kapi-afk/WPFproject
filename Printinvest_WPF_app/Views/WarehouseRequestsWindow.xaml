<Window x:Class="Printinvest_WPF_app.Views.WarehouseRequestsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Заявки мастеров"
        Height="520"
        Width="980"
        MinHeight="520"
        MinWidth="980"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource ContentBackgroundBrush}">

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Заявки мастеров"
                   FontSize="24"
                   FontWeight="Bold"
                   Margin="0,0,0,10"/>

        <TextBlock Grid.Row="1"
                   TextWrapping="Wrap"
                   Foreground="{DynamicResource SecondaryTextBrush}"
                   Margin="0,0,0,12"/>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding WarehouseRequests}"
                  SelectedItem="{Binding SelectedWarehouseRequest}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  HorizontalScrollBarVisibility="Auto">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Заявка" Binding="{Binding OrderDisplayNumber}" Width="0.8*"/>
                <DataGridTextColumn Header="Мастер" Binding="{Binding Master.Name}" Width="1.1*"/>
                <DataGridTextColumn Header="Материал" Binding="{Binding RequestedItemName}" Width="1.4*"/>
                <DataGridTextColumn Header="Категория" Binding="{Binding RequestedCategory}" Width="1.1*"/>
                <DataGridTextColumn Header="Кол-во" Binding="{Binding RequestedQuantity}" Width="0.7*"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="1*"/>
                <DataGridTemplateColumn Header="" Width="130">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Закрыть"
                                    Width="104"
                                    Height="28"
                                    HorizontalAlignment="Center"
                                    Command="{Binding DataContext.ResolveWarehouseRequestCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                    CommandParameter="{Binding}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
